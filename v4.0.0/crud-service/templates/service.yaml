apiVersion: v1
kind: Service
metadata:
  name: {{ include "crud-service.fullname" . }}
  labels:
    {{- include "crud-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crud-service.selectorLabels" . | nindent 4 }}

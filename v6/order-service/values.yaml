# Default values for order-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: vldmr/order-service

service:
  type: NodePort
  port: 8000

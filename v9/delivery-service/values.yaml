# Default values for delivery-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: vldmr/delivery-service

service:
  type: NodePort
  port: 8000

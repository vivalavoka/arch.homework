# Default values for billing-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: vldmr/billing-service

service:
  type: NodePort
  port: 8000
